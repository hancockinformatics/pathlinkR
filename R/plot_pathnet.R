plot_pathnet <- function() {

  #' - Create wrapped version of pathway name to label nodes
  #' - Create "dummy" column so all nodes have coloured outline, but only
  #'   enriched nodes have the same fill colour

  # mutate(
  #   pathway_name_1_wrap = map_chr(
  #     as.character(pathway_name_1),
  #     ~str_wrap(.x, width = 20)
  #   ),
  #   pathway_name_1_wrap = str_replace(pathway_name_1_wrap, "^$", NA_character_)
  # )

}
